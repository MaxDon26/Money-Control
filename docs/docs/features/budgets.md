---
sidebar_position: 4
---

# Бюджеты

## Описание

Бюджеты позволяют планировать расходы и контролировать лимиты.

## Типы бюджетов

### По категории

Лимит на конкретную категорию расходов:
- Продукты: 30 000 ₽/мес
- Развлечения: 10 000 ₽/мес

### Общий бюджет

Лимит на все расходы за месяц:
- Общий бюджет: 100 000 ₽/мес

## Функции

### Создание бюджета

- Выбор категории (или общий)
- Установка лимита
- Период: месяц + год

### Прогресс

Для каждого бюджета рассчитывается:
- `spent` — потрачено
- `percentage` — процент от лимита
- `remaining` — осталось

### Визуальные предупреждения

| Процент | Статус | Цвет |
|---------|--------|------|
| < 80% | Норма | Зелёный |
| 80-99% | Предупреждение | Жёлтый |
| ≥ 100% | Превышен | Красный |

### Копирование

Можно скопировать бюджеты из предыдущего месяца.

## API

```http
GET    /api/budgets               # Список
POST   /api/budgets               # Создать
PATCH  /api/budgets/:id           # Обновить
DELETE /api/budgets/:id           # Удалить
GET    /api/budgets/progress      # Прогресс с расчётами
POST   /api/budgets/copy-previous # Копировать из прошлого месяца
```

## Примеры

### Создание бюджета по категории

```json
POST /api/budgets
{
  "categoryId": "category-uuid",
  "amount": 30000,
  "month": 1,
  "year": 2024
}
```

### Создание общего бюджета

```json
POST /api/budgets
{
  "isTotal": true,
  "amount": 100000,
  "month": 1,
  "year": 2024
}
```

### Ответ прогресса

```json
GET /api/budgets/progress?month=1&year=2024

{
  "budgets": [
    {
      "id": "uuid",
      "category": { "id": "...", "name": "Продукты", "icon": "🛒" },
      "amount": 30000,
      "spent": 25000,
      "percentage": 83.3,
      "remaining": 5000
    }
  ],
  "totalBudget": {
    "id": "uuid",
    "amount": 100000,
    "spent": 75000,
    "percentage": 75,
    "remaining": 25000
  },
  "summary": {
    "totalBudget": 100000,
    "totalSpent": 75000,
    "overallPercentage": 75
  }
}
```

### Копирование

```json
POST /api/budgets/copy-previous?month=2&year=2024

{
  "copied": 5,
  "created": 5
}
```
