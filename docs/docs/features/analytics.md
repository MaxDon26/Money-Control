---
sidebar_position: 6
---

# Аналитика

## Описание

Визуализация и анализ финансовых данных.

## Периоды

| Период | Описание |
|--------|----------|
| Месяц | Текущий месяц |
| Квартал | Текущий квартал |
| Год | Текущий год |
| Произвольный | Выбранный диапазон дат |

## Виджеты дашборда

### Общий баланс

Сумма всех активных счетов.

### Доходы/Расходы за месяц

Суммы за текущий месяц.

### Экономия

`Доходы - Расходы` за период.

### Круговая диаграмма

Расходы по категориям в процентах.

### График тренда

Динамика доходов/расходов по месяцам (6 или 12 месяцев).

### Топ категорий

Рейтинг категорий по сумме расходов.

### Последние транзакции

5 последних операций.

### Регулярные платежи

Виджет отображает все активные регулярные платежи:
- Карточки с названием, иконкой категории, суммой и периодичностью
- Дата следующего платежа
- В заголовке — суммарные ежемесячные расходы и доходы

**Расчёт месячной суммы:**

| Периодичность | Формула |
|---------------|---------|
| Ежедневно | сумма × 30 |
| Еженедельно | сумма × 4.33 |
| Ежемесячно | сумма |
| Ежегодно | сумма ÷ 12 |

## API

```http
GET /api/analytics/summary        # Общая статистика
GET /api/analytics/by-category    # По категориям
GET /api/analytics/trend          # Тренд по месяцам
GET /api/analytics/top-categories # Топ категорий
GET /api/analytics/recent         # Последние транзакции
```

## Примеры

### Сводка

```json
GET /api/analytics/summary?dateFrom=2024-01-01&dateTo=2024-01-31

{
  "totalBalance": 250000,
  "income": 150000,
  "expense": 85000,
  "savings": 65000
}
```

### По категориям

```json
GET /api/analytics/by-category?dateFrom=2024-01-01&dateTo=2024-01-31

{
  "expenses": [
    {
      "category": { "id": "...", "name": "Продукты", "icon": "🛒" },
      "amount": 25000
    },
    {
      "category": { "id": "...", "name": "Транспорт", "icon": "🚗" },
      "amount": 15000
    }
  ],
  "incomes": [
    {
      "category": { "id": "...", "name": "Зарплата", "icon": "💰" },
      "amount": 150000
    }
  ]
}
```

### Тренд

```json
GET /api/analytics/trend?months=6

[
  { "month": "Авг", "income": 150000, "expense": 80000, "balance": 70000 },
  { "month": "Сен", "income": 150000, "expense": 95000, "balance": 55000 },
  { "month": "Окт", "income": 160000, "expense": 85000, "balance": 75000 },
  { "month": "Ноя", "income": 150000, "expense": 90000, "balance": 60000 },
  { "month": "Дек", "income": 180000, "expense": 120000, "balance": 60000 },
  { "month": "Янв", "income": 150000, "expense": 85000, "balance": 65000 }
]
```

### Топ категорий

```json
GET /api/analytics/top-categories?limit=5

[
  { "category": { "id": "...", "name": "Продукты" }, "amount": 25000 },
  { "category": { "id": "...", "name": "Транспорт" }, "amount": 15000 },
  { "category": { "id": "...", "name": "Развлечения" }, "amount": 10000 },
  { "category": { "id": "...", "name": "Рестораны" }, "amount": 8000 },
  { "category": { "id": "...", "name": "Связь" }, "amount": 5000 }
]
```

## Графики

### Recharts компоненты

- `PieChart` — круговая диаграмма расходов
- `BarChart` — столбчатый график тренда
- `LineChart` — линейный график динамики
- `ResponsiveContainer` — адаптивный контейнер
