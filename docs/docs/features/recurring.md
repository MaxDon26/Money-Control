---
sidebar_position: 5
---

# Регулярные платежи

## Описание

Автоматизация регулярных доходов и расходов: подписки, аренда, зарплата и другие периодические операции.

## Периодичность

| Частота | Описание |
|---------|----------|
| `DAILY` | Ежедневно |
| `WEEKLY` | Еженедельно |
| `MONTHLY` | Ежемесячно |
| `YEARLY` | Ежегодно |

## Примеры использования

- Зарплата (ежемесячно)
- Аренда квартиры (ежемесячно)
- Подписка на Netflix (ежемесячно)
- Страховка (ежегодно)

## Функции

### Создание платежа

- Название
- Сумма
- Тип (доход/расход)
- Категория
- Счёт
- Периодичность
- Дата начала
- Дата окончания (опционально)

### Выполнение

**Ручное:**
- Кнопка "Выполнить сейчас"
- Создаётся транзакция
- `nextDate` сдвигается вперёд

**Автоматическое:**
- Cron задача проверяет ежедневно
- Если `nextDate <= сегодня` → выполняется

### Пропуск

- Кнопка "Пропустить"
- `nextDate` сдвигается без создания транзакции
- Полезно для отпуска, задержки зарплаты

### Приостановка

- Переключатель "Активен"
- Неактивные платежи не выполняются
- Можно возобновить

## API

```http
GET    /api/recurring              # Список
POST   /api/recurring              # Создать
PATCH  /api/recurring/:id          # Обновить
DELETE /api/recurring/:id          # Удалить
POST   /api/recurring/:id/toggle   # Вкл/выкл
POST   /api/recurring/:id/process  # Выполнить
POST   /api/recurring/:id/skip     # Пропустить
```

## Примеры

### Создание

```json
POST /api/recurring
{
  "name": "Зарплата",
  "amount": 150000,
  "type": "INCOME",
  "categoryId": "salary-category-uuid",
  "accountId": "card-account-uuid",
  "frequency": "MONTHLY",
  "startDate": "2024-01-10T00:00:00.000Z"
}
```

### Ответ

```json
{
  "id": "uuid",
  "name": "Зарплата",
  "amount": "150000.00",
  "type": "INCOME",
  "frequency": "MONTHLY",
  "startDate": "2024-01-10T00:00:00.000Z",
  "nextDate": "2024-02-10T00:00:00.000Z",
  "isActive": true,
  "category": { "id": "...", "name": "Зарплата", "icon": "💰" },
  "account": { "id": "...", "name": "Сбербанк" }
}
```

### Выполнение

```json
POST /api/recurring/uuid/process

{
  "transaction": {
    "id": "new-transaction-uuid",
    "amount": "150000.00",
    "date": "2024-02-10T00:00:00.000Z"
  },
  "nextDate": "2024-03-10T00:00:00.000Z"
}
```

### Пропуск

```json
POST /api/recurring/uuid/skip

{
  "skipped": true,
  "previousDate": "2024-02-10T00:00:00.000Z",
  "nextDate": "2024-03-10T00:00:00.000Z"
}
```
