---
sidebar_position: 1
---

# Счета

## Описание

Счета — основа учёта финансов. Каждая транзакция привязана к счёту.

## Типы счетов

| Тип | Описание | Пример |
|-----|----------|--------|
| `CASH` | Наличные | Кошелёк |
| `CARD` | Банковская карта | Сбербанк, Тинькофф |
| `DEPOSIT` | Вклад/накопительный | Сберегательный счёт |
| `CREDIT` | Кредитная карта | Кредитка |
| `INVESTMENT` | Инвестиции | Брокерский счёт |

## Функции

### Создание счёта

- Название (обязательно)
- Тип счёта
- Начальный баланс
- Валюта (RUB, USD, EUR)
- Иконка (emoji)
- Цвет

### Редактирование

Можно изменить все параметры кроме типа.

### Архивирование

Вместо удаления счёт архивируется:
- Транзакции сохраняются
- Счёт не отображается в списках
- Баланс не учитывается в общей сумме

### Общий баланс

Сумма балансов всех активных счетов отображается на дашборде.

## API

```http
GET    /api/accounts         # Список счетов
POST   /api/accounts         # Создать счёт
GET    /api/accounts/:id     # Получить счёт
PATCH  /api/accounts/:id     # Обновить счёт
DELETE /api/accounts/:id     # Архивировать счёт
GET    /api/accounts/total   # Общий баланс
```

## Примеры

### Создание счёта

```json
POST /api/accounts
{
  "name": "Сбербанк",
  "type": "CARD",
  "balance": 50000,
  "currency": "RUB",
  "icon": "💳",
  "color": "#52c41a"
}
```

### Ответ

```json
{
  "id": "uuid",
  "name": "Сбербанк",
  "type": "CARD",
  "balance": "50000.00",
  "currency": "RUB",
  "icon": "💳",
  "color": "#52c41a",
  "isArchived": false,
  "createdAt": "2024-01-15T10:00:00.000Z"
}
```
