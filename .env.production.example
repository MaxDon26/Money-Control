# Database
DB_USER=money_user
DB_PASSWORD=CHANGE_ME_STRONG_PASSWORD
DB_NAME=money_db

# JWT (generate with: openssl rand -base64 32)
JWT_SECRET=CHANGE_ME_GENERATE_RANDOM_STRING_32_CHARS

# API URL (for frontend build)
API_URL=/api

# Domain (for SSL)
DOMAIN=your-domain.com
